version: '3'

services:
  website:
    image: tourguide-website
    build:
      context: .
    ports:
      - '8000:8000'
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
